<Window x:Class="Zara.Desktop.Overlays.LockErrorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:ClassModifier="internal"
        Width="480"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        Topmost="True"
        FocusManager.FocusedElement="{Binding ElementName=ConfirmButton}"
        Background="#FF242832">
  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <TextBlock x:Name="MessageText"
               Foreground="White"
               FontSize="15"
               TextWrapping="Wrap" />
    <StackPanel Grid.Row="1"
                Margin="0,24,0,0"
                HorizontalAlignment="Right"
                Orientation="Horizontal">
      <Button x:Name="RetryButton"
              MinWidth="100"
              Height="36"
              Margin="0,0,8,0"
              Padding="16,0"
              Click="Retry_Click"
              Content="다시 시도" />
      <Button x:Name="ConfirmButton"
              MinWidth="100"
              Height="36"
              Padding="16,0"
              IsDefault="True"
              IsCancel="True"
              Click="Confirm_Click"
              Content="확인" />
    </StackPanel>
  </Grid>
</Window>
