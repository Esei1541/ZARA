<Window x:Class="Zara.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ZARA"
        Width="620"
        Height="300"
        MinWidth="540"
        MinHeight="280"
        Icon="Assets/ZaraIcon.png"
        Background="#FFF7F8FA"
        WindowStartupLocation="CenterScreen">
  <Grid Margin="32">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="32" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="24" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <DockPanel LastChildFill="True">
      <Border Width="52"
              Height="52"
              Margin="0,0,16,0"
              Padding="8"
              Background="#FF171A21"
              CornerRadius="12"
              DockPanel.Dock="Left">
        <Image Source="Assets/ZaraIcon.png" Stretch="Uniform" />
      </Border>
      <TextBlock VerticalAlignment="Center"
                 FontSize="26"
                 FontWeight="SemiBold"
                 Text="ZARA" />
    </DockPanel>

    <CheckBox Grid.Row="2"
              Command="{Binding ToggleRestartSettingCommand}"
              Content="평상시에도 프로그램이 종료되면 재실행"
              IsChecked="{Binding RestartOnExitWhenUnlocked, Mode=OneWay}"
              VerticalContentAlignment="Center" />

    <Button Grid.Row="4"
            MinWidth="156"
            Height="36"
            Padding="16,0"
            HorizontalAlignment="Right"
            Command="{Binding StartLockDemoCommand}"
            Content="오버레이 시연 시작"
            FontWeight="SemiBold" />
  </Grid>
</Window>
